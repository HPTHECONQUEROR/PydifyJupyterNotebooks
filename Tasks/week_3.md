Create a tuple which has three elements in it and unpack it with three variables.
Create a tuple containing the names of your five favorite fruits. Print the tuple, and then print the third fruit in the tuple.
Create a tuple containing the numbers from 1 to 10. Use a slice to print the first five numbers, and then use a slice to print the last five numbers.
Create a tuple containing the names of five cities. Use the max() function to find the city with the longest name, and then use the min() function to find the city with the shortest name.


Mini calci - +,-,*,/,//,**,%,

Write a function that takes a list of numbers as input and returns the sum of all the numbers in the list. Implement the solution using a for loop.

IP: [1,2,3,4]
OP: 10
Write a recursive function to calculate the factorial of a given positive integer.

IP: 4
OP: 24

Write a function that takes a string as input and returns the reverse of the string. Implement the solution using a while loop.

IP: 'FAB'
OP: 'BAF'

Write a lambda function that takes a list of numbers as input and returns a new list containing only the even numbers.

IP: [1,2,3,4,5,6]
OP: [2, 4, 6]

Write a recursive function to calculate the value of 2 raised to the power of a given non-negative integer.

IP: 10
OP: 1024

Write a recursive function/ for loop that takes a positive integer as input and prints a countdown from that number to 1.

IP : 5
OP : 5 4 3 2 1 